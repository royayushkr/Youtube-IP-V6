[client]
# Keep the host toolbar out of the app chrome so page titles start cleanly below the viewport edge.
toolbarMode = "minimal"

[theme]
base = "light"
primaryColor = "#FF0000"
backgroundColor = "#FFFFFF"
secondaryBackgroundColor = "#F9F9F9"
textColor = "#0F0F0F"
font = "sans serif"
